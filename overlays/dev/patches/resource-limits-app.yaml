apiVersion: apps/v1
kind: Deployment
metadata:
  name: myreadings-app
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: myreadings-app
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 1Gi
